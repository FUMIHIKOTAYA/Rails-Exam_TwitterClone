<h3>以下の内容で、投稿する。</h3>

<%= form_with(model: @post, url: confirm_new_or_edit, method: confirm_form_method, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>本文：<%= @post.content %></p>

  <%= form.submit '投稿する' %>
  <%= form.submit '戻る', name: 'back' %>
<% end %>
